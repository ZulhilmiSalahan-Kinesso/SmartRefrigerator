import { Photo } from './photo';

export class Item {
    id?: string;
    category: string;
    name: string;
    image: string;
    barcode: string;
    expiredDate: Date;
    createdAt: number;
  // tslint:disable:eofline
  }